{% extends "__layout.twig" %}
{# {% block menu %}
dhdhgh
{% endblock %} #}
{% block content %}
{# <div style="display: flex;justify-content: space-between; margin: 20px;"> #}
<head>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css"
    integrity="sha384-gfdkjb5BdAXd+lj+gudLWI+BXq4IuLW5IT+brZEZsLFm++aCMlF1V92rMkPaX4PP" crossorigin="anonymous">
</head>

<div>
    {{title}}
</div>
{# <pre>
    {{ dump(user) }}
</pre> #}

{# <div class="row d-flex">
{% for object in web_objects %}
<div class="col mb-4 d-flex align-items-stretch">
    <div class="border p-2 d-flex flex-column justify-content-between position-relative">
        <div class="position-absolute" style="right: 4px; top: 4px;">
            <form action="/web-object/{{object.id}}/delete" method="post">
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-times"></i>
                </button>
            </form>
        </div>
        <div class="position-absolute" style="right: 43px; top: 4px;">
            <form action="/web-object/{{object.id}}/edit" method="get">
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-pen-alt"></i>
                </button>
            </form>
        </div>
        <div class="img-thumbnail mb-2" style="width: 100%; height: 220px; overflow:hidden">
            <img style="width: 100%; height: 100%; object-fit:cover;" src="{{object.image}}" alt="">
        </div>
        <div class="d-flex flex-column">
            <a class="btn btn-primary mb-1"  href="/web_object/{{object.id}}">{{ object.title }}</a>
            <div class="d-flex justify-content-center">
                <a class="me-3" href="/web_object/{{object.id}}?show=image">Картинка</a>
                <a href="/web_object/{{object.id}}?show=info">Описание</a>
            </div>
        </div>
    </div>
</div>
{% endfor %}
</div> #}

{% endblock %}